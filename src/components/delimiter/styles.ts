import { colors } from "../../theme";

export const styles = {
  delimiter: {
    border: `2px solid ${colors.mandarineOrange}`,
    borderRadius: "10px",
    display: "block",
    width: "100%",
  },
};
