mixin chat-preview
    script(type="module").
      import ChatPreview from "/src/components/chat-preview/chat-preview.ts";
      document.currentScript.parentNode.innerHTML += (new ChatPreview()).element
