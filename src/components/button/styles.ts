import { colors } from "../../theme";

export const styles = {
  button: {
    backgroundColor: colors.mandarineOrange,
    border: 0,
    borderRadius: "16px",
    color: "#ffffff",
    cursor: "pointer",
    fontFamily: "Assistant, sans-serif",
    fontSize: "24px",
    padding: "6px 31px",
    textAlign: "center",
    textDecoration: "none",
  },
};
