mixin button(text, url)
    script(type="module").
      import Button from "/src/components/button/button.ts";
      document.currentScript.parentNode.innerHTML += (new Button({
      text: "#{text}",
      url: "#{url}"
      })).element
