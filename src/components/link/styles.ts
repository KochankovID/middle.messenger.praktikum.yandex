import { fonts } from "../../theme";

export const styles = {
  link: {
    ...fonts.linkFont,
    alignItems: "center",
    cursor: "pointer",
    display: "flex",
    justifyContent: "center",
    position: "relative",
    height: "100%",
    left: 0,
    top: 0,
    textDecoration: "none",
  },
};
