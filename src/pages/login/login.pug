extends ../../layout/main/main

include ../../components/form/form
include ../../components/button/button
include ../../components/input/input
include ../../components/link/link
include ../../components/delimiter/delimiter

block content
    link(rel="stylesheet" href="login.sass")
    .login-container
        +form('Chat app')
            h3 Вход
            label(for="login") Логин
            +input("login", "text", "login")
            +delimiter
            label(for="password") Пароль
            +input("password", "password", "password")
            +delimiter
            +button('Авторизоваться', '../chat-not-selected/chat-not-selected.html')
            +link('/registration/registration.html')
                p Нет аккаунта?
    script(type="module", src="./login.ts")
